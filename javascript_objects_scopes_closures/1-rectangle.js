/*a class Rectangle 
that defines a rectangle:*/
class Rectangle {
    constructor(w, h); {
        this.width = w; 
        this.height = h;
    }
    
}

const Rectangle = new Rectangle(w, h);
console.log(`Rectangle is ${Rectangle}`);
