#!/usr/bin/node
/*creates a JavaScript object updates
 and then prints the object to the console.*/
const myObject = {
    type: 'object',
    value: 12
  };
  console.log(myObject);
  
  myObject.value = 89; //update value


  console.log(myObject);
